---
permalink: /feed.xml
eleventyExcludeFromCollections: true
---
{% set posts = collections.posts | slice(0, 20) %}
{{ posts | rssFeed({
  title: site.name,
  description: site.description,
  site: site.url
}) | safe }}